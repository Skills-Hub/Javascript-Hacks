class User {
    constructor(id, username, email) {
        this.id = id;
        this.username = username;
        this.email = email;
        // Add other fields as needed
    }
}

module.exports = User;
